<%= form_with(model: private_sector) do |form| %>
  <% if private_sector.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(private_sector.errors.count, "error") %> Errore!&emsp;Non posso salvare per i seguenti motivi:</h2>
      <ul>
        <% private_sector.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="input-group mb-2">
    <span class="input-group-text input-group-text-fixed">
      <i class="fa-regular fa-building"></i>
      &nbsp;&nbsp;
      <%= form.label :name %>
    </span>
    <%= form.text_field :name, class: "form-control" %>
  </div>
  <div class="input-group mb-2">
    <span class="input-group-text input-group-text-fixed">
      <i class="fa-solid fa-arrow-up-1-9"></i>
      &nbsp;&nbsp;
      <%= form.label :position %>
    </span>
    <%= form.number_field :position, class: "form-control" %>
  </div>
  <div class="d-grid">
    <%= form.submit class: "btn btn-primary" %>
  </div>
<% end %>
